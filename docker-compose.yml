version: "3.8"

services:
  ua-nav-access:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=your_openai_key
      - LLM_BASE=https://your-llm-api-base
    volumes:
      - .:/app
